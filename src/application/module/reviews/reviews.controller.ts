import { NextFunction, Request, Response } from "express";

const ReviewHandler = {
  createReview: (request: Request, response: Response, next: NextFunction) => {
    try {
    } catch (error) {
      next(error);
    }
  },

  updateReview: (request: Request, response: Response, next: NextFunction) => {
    try {
    } catch (error) {
      next(error);
    }
  },

  deleteReview: (request: Request, response: Response, next: NextFunction) => {
    try {
    } catch (error) {
      next(error);
    }
  },

  getAllReview: (request: Request, response: Response, next: NextFunction) => {
    try {
    } catch (error) {
      next(error);
    }
  },

  getReviewAvgRating: (
    request: Request,
    response: Response,
    next: NextFunction
  ) => {
    try {
    } catch (error) {
      next(error);
    }
  },
};

export default ReviewHandler;